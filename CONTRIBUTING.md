# Contributing

Rule 0 (non-negotiable): **No secrets, no real credentials, sandbox-only runs.**

Do not commit `.env`, tokens/keys, raw artifacts, or any payloads/logs that could contain secrets. Run only in a local, loopback-only environment with sandbox identities.

